{
  "name": "studyfind-frontend",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "install": "lerna run install"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
