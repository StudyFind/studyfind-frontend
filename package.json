{
  "name": "studyfind-frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "initialize": "lerna run bootstrap && lerna run build && lerna run bootstrap",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "install": "lerna run install"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
